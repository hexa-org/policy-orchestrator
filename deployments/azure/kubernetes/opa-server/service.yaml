apiVersion: v1
kind: Service
metadata:
  name: hexa-demo-opa-server-agent
  labels:
    app: hexa-demo-opa-server-agent
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8881
  selector:
    app: hexa-demo-opa-server-agent
    tier: frontend
